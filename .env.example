# Ordit — plantilla de variables d'entorn. Copia-la a .env i ompli els valors.
# Mai committeges .env. L'acces es fa estil python-decouple, mai os.environ directe.

# Ruta del fitxer DuckDB que usa el teler (dbt). Relativa a ordit_dbt/ o absoluta.
DUCKDB_PATH=../data/ordit.duckdb
