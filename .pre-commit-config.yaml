# Pre-commit: forca ruff (check + format) abans de cada commit.
# Instal·la amb `just setup` (uv sync + pre-commit install).
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
